[package]
name = "keel-extension-sdk"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dependencies]
keel-protocol = { path = "../keel-protocol" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

